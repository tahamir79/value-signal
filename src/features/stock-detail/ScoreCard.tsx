export function ScoreCard({ label, value, inverse = false }: { label: string; value: number | null; inverse?: boolean }) { const width = value ?? 0; return <div className="score-card"><div><span>{label}</span><strong>{value ?? "—"}</strong></div><div className="score-track" aria-label={`${label}: ${value ?? "not available"}`}><i className={inverse ? "inverse" : ""} style={{width:`${width}%`}}/></div></div>; }
