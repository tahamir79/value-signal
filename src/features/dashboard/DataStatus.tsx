import type { EtlReport } from "@/lib/etl";
export function DataStatus({report}:{report:EtlReport}){const live=report.status!=="not_run";return <aside className={`data-status ${live?"data-live":"data-fixture"}`}><div><span>DATA PIPELINE</span><strong>{live?report.status.replace("_"," "):"Fixture mode"}</strong></div><p>{live?`${report.successfulTickers} companies refreshed; ${report.failedTickers} failed. Last run ${new Date(report.runFinishedAt!).toLocaleString("en-US",{dateStyle:"medium",timeStyle:"short",timeZone:"UTC"})} UTC.`:"Live ETL has not run yet. The interface is using Phase 01 placeholder observations."}</p></aside>}
