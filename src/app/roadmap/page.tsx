import{phases}from"@/data/phases";import{PhaseCard}from"@/components/PhaseCard";export default function Roadmap(){return <div className="page"><div className="page-head"><span className="kicker">MASTER PLAN / 10 PHASES</span><h1>Roadmap</h1><p>Each phase produces an independently testable layer. Finish the contract before the pipeline; finish retrieval before generation.</p></div><div className="phase-list">{phases.map(p=><PhaseCard key={p.id} phase={p}/>)}</div></div>}
