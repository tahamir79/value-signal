const layers=[{n:"01",t:"Acquire",d:"Market prices · SEC facts · 10-K / 10-Q filings",m:"Python providers + cache"},{n:"02",t:"Normalize",d:"Canonical symbols · units · periods · clean filing text",m:"Validation + lineage"},{n:"03",t:"Analyze",d:"Value · quality · momentum · balance-sheet risk",m:"Features + scores"},{n:"04",t:"Evaluate",d:"Point-in-time signals · forward returns · benchmarks",m:"Backtesting lab"},{n:"05",t:"Retrieve",d:"Section chunks · inverted index · BM25 · citations",m:"Retrieval engine"},{n:"06",t:"Present",d:"Dashboard · stock pages · deterministic briefs",m:"Next.js static data UI"}];
export default function Architecture(){return <div className="page"><div className="page-head"><span className="kicker">SYSTEM BLUEPRINT / STATIC-FIRST</span><h1>Architecture</h1><p>A separable pipeline: every boundary emits an inspectable artifact, so one weak module cannot hide inside another.</p></div><section className="flow">{layers.map((x,i)=><div className="flow-row" key={x.n}><b>{x.n}</b><div><span>{x.t}</span><p>{x.d}</p></div><code>{x.m}</code>{i<layers.length-1&&<i>↓</i>}</div>)}</section><div className="architecture-grid"><ArchitectureBlock k="ETL CONTROL PLANE" t="Data architecture" d="Provider adapters isolate external formats. Raw responses are cached; normalized records retain source and run metadata. A single runner emits frontend JSON plus a machine-readable audit report." code={"providers → raw cache → cleaners → feature table\nfeature table → scorer → signals.json"}/><ArchitectureBlock k="EVALUATION PLANE" t="Backtesting architecture" d="Historical snapshots use only information available by the signal date. Outcomes live separately and join only after the prediction timestamp." code={"point-in-time features + score version\n→ signal snapshot → future outcomes → evaluation"}/><ArchitectureBlock k="EVIDENCE PLANE" t="Retrieval architecture" d="Filings remain authoritative. Section-aware chunks preserve accession metadata; lexical retrieval is measured before local semantic search is added." code={"SEC HTML → clean text → cited chunks\nquery → BM25 / hybrid → ranked evidence"}/><ArchitectureBlock k="DELIVERY PLANE" t="Frontend & deployment" d="Next.js consumes versioned static JSON, keeping the free MVP inexpensive and debuggable. Vercel serves the UI; scheduled ETL can publish artifacts through CI." code={"public/data/*.json → typed loaders → views\nGitHub Actions → checks → Vercel"}/></div><section className="detail-section"><h2>Planned repository</h2><pre className="tree">{"value-signal-lite/\n├── scripts/          # providers, ETL, features, scoring\n├── public/data/      # versioned UI contracts\n├── src/app/          # dashboard, stock, methodology\n├── src/components/   # decision-support views\n├── src/lib/          # loaders, search, brief rules\n├── tests/            # fixtures and invariants\n└── docs/             # dictionaries and runbooks"}</pre></section><section className="statement"><span>FUTURE SCALE</span><p>Move artifacts to object storage, adopt Parquet for analysis, orchestrate incremental jobs, and add an API only when static delivery becomes a measured constraint.</p></section></div>}
function ArchitectureBlock({k,t,d,code}:{k:string;t:string;d:string;code:string}){return <section><span className="kicker">{k}</span><h2>{t}</h2><p>{d}</p><pre>{code}</pre></section>}
